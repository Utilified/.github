name: _claude-selftest

# TEMPORARY: validates that reusable-claude.yml compiles and starts (no
# startup_failure) using the same caller shape the product repos use.
# Removed before merging the fix.
on:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/reusable-claude.yml
    secrets:
      claude-code-oauth-token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
